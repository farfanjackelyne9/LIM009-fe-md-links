#!/usr/bin/env node 
//const [,,  ...args] = process.argv; 
//console.log(`link ingresado ${args}`); // Te pinta en la consola lo introducido.







/*const extractLinks = (route) =>{
    let dir = fs.readFileSync(route);
    let readFile = dir.toString();
    let x = new marked.Renderer(readFile);
    console.log(x);
   
}
extractLinks(route)*/




  
